# 移动端菜单修复说明

## 修复内容

已修复移动端（屏幕宽度 ≤ 960px）顶部菜单栏的显示问题，并优化了移动端用户体验。

## 修改的文件

### 1. `assets/css/style.css`
- 添加了汉堡菜单按钮样式 (`.mobile-menu-toggle`)
- 优化了移动端导航菜单的响应式布局
- 菜单默认隐藏，点击汉堡按钮后展开
- 下拉菜单在移动端改为垂直展开
- **语言切换器和操作按钮在移动端移入菜单内**，用分隔线区分

### 2. `index.html`
- 在 header 中添加了汉堡菜单按钮
- 按钮包含三条横线的图标，点击后会变成 X 形状
- 修复了 nav 标签的闭合位置

### 3. `about.html`
- 同样添加了汉堡菜单按钮，保持一致性

### 4. `assets/js/app.js`
- 添加了 `setupMobileMenu()` 函数
- 实现了菜单的展开/收起功能
- 点击菜单项后自动关闭菜单
- 点击操作按钮后也会自动关闭菜单
- 点击页面其他区域也会关闭菜单
- 处理了下拉菜单在移动端的交互

## 功能特性

✅ 汉堡菜单按钮（三条横线图标）  
✅ 点击按钮展开/收起菜单  
✅ 菜单展开时按钮变成 X 形状  
✅ 点击菜单项后自动关闭  
✅ 点击操作按钮后自动关闭  
✅ 点击外部区域自动关闭  
✅ 下拉菜单在移动端可点击展开  
✅ **语言切换器在移动端显示在菜单底部**  
✅ **"See the roadmap" 和 "Join the movement" 按钮在移动端显示在菜单底部**  
✅ **用分隔线将导航链接和操作区域区分开**  
✅ 无障碍支持（aria-label 和 aria-expanded）  

## 移动端布局

在移动端（≤ 960px），菜单结构如下：

```
┌─────────────────────────┐
│ Logo    [☰]            │  ← 顶部栏
├─────────────────────────┤
│ Home                    │
│ Projects                │
│ Standards               │
│ Activities              │
│ Developers              │
│ Open Lab                │
│ Training & Certification│
│ About Us ▼              │
│   └─ Foundation overview│
│   └─ Mission            │
│   └─ Charter            │
│   └─ Organization...    │
│   └─ Donor directory    │
├─────────────────────────┤  ← 分隔线
│ [语言选择器]            │
│ [See the roadmap]       │
│ [Join the movement]     │
└─────────────────────────┘
```

## 测试方法

1. 在浏览器中打开 `index.html` 或 `about.html`
2. 将浏览器窗口缩小到 960px 以下（或使用开发者工具的移动设备模拟）
3. 应该能看到汉堡菜单按钮（三条横线）
4. 点击按钮，菜单应该展开，包含所有导航链接
5. 滚动到菜单底部，应该能看到：
   - 分隔线
   - 语言切换下拉框
   - "See the roadmap" 按钮
   - "Join the movement" 按钮
6. 点击任何菜单项或按钮，菜单应该自动关闭

## 兼容性

- 支持所有现代浏览器
- 响应式设计，适配各种移动设备
- 平滑的动画过渡效果
- 桌面端（> 960px）保持原有布局不变
